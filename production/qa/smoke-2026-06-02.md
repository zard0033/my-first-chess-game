## Smoke Check Report

**Date**: 2026-06-02
**Sprint**: 10 — Game Replay (Phase 2)
**Engine**: Web App (Vitest) · Stockfish 18 Lite single-threaded
**QA Plan**: `production/qa/qa-plan-sprint-10-2026-06-01.md`
**Argument**: sprint

---

### Automated Tests

**Status**: PASS — 492 tests, 492 passing, 0 failing (41 suites).
Command: `npx vitest run`. `vue-tsc --noEmit` + `vite build` green.

New this sprint (+36): replay-positions (6), replay-analysis integration (5),
replay-navigation (8), replay-view (11), game-replay-rating (6).

---

### Test Coverage

| Story | Type | Test File | Status |
|-------|------|-----------|--------|
| S10-01 pgn-viewer wrapper | UI | `tests/unit/components/pgn-viewer.test.ts` | COVERED |
| S10-02 ReplayView + nav | Logic/UI | `tests/unit/views/replay-view.test.ts`, `tests/unit/game-replay/replay-navigation.test.ts` | COVERED |
| S10-03 analysis overlay | Integration | `tests/integration/game-replay/analysis.test.ts`, `tests/unit/game-replay/replay-positions.test.ts` | COVERED |
| S10-04 rating | Logic | `tests/unit/components/game-replay-rating.test.ts` | COVERED |
| S10-05 animation | Visual/Feel | — | DEFERRED (nice-to-have, advisory) |
| S10-06 engine migration | Foundation | `tests/unit/chess-engine/review-engine.test.ts` + browser verification | COVERED |

**Summary**: 5 covered, 1 deferred (nice-to-have), 0 missing.

---

### Manual Smoke Checks (browser, Playwright-driven)

- [x] App launches, navigates, no crash — PASS
- [x] **Play**: human move e2e4 → engine (SF18 Lite) replied e7e5 — PASS
- [x] **Replay**: `/replay/:gameId` renders board + move list + controls — PASS
- [x] **Replay nav**: keyboard ←/→ steps ply (0→3), move-list click syncs (→9), slider/buttons — PASS, no desync/loop (post code-review H1 fix)
- [x] **Replay analysis**: depth-12 pre-analysis 11/11 positions ~6s; eval bar updates per move (+0.4 → −0.4 → −0.2); best-move arrow renders (b8→c6) — PASS
- [x] **Engine load**: `stockfish-18-lite-single` handshake + bestmove; NNUE embedded (`nn-9067e33176e8.nnue`, 11MiB) — PASS
- [-] Performance / 60fps profiling — not formally profiled (S10-05 deferred); no visible jank during rapid stepping
- Known cosmetic: pgn-viewer CSS embeds a base64 icon font blocked by CSP `default-src 'self'` (no `font-src`) — board/coords render fine; optional CSP `font-src 'self' data:` cleanup later (ADR-0008).

---

### Verdict: PASS

All Must-Have + Should-Have stories covered by passing automated tests and verified
end-to-end in a real browser. The one gap (S10-05 animation) is a deferred nice-to-have
with an advisory-only gate. Build is ready for QA sign-off.

Note: this sprint's browser smoke surfaced and fixed a latent defect (SF16 NNUE network
never deployed → review/replay analysis silently hung in-browser) by migrating to
Stockfish 18 Lite — see ADR-0001 amendment (2026-06-02) and S10-06.
